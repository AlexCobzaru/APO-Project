package org.example;

public class Admin extends User {
    public Admin(int userId, String nume, String email) {
        super(userId, nume, email);
    }
    // Funcții specifice unui Admin pot fi adăugate aici
}

